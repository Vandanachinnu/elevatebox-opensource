def rotate_array(N, arr):
  first_element = arr[0]
  
  for i in range(1, N):
    arr[i - 1] = arr[i]
    
    
  arr[N - 1] = first_element
  
  print(" ".join(map(str, arr)))
  
N = int(input())  
arr = list(map(int, input().split()))

rotate_array(N, arr)
